<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Bank_App.TransationsPage">
    <ContentPage.Content>
        <StackLayout>

            <ListView ItemsSource="{Binding Transactions}">
                     <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*">
                                </ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Label TextColor="Black" Grid.Column="0" Grid.Row="0" Text="{Binding Description}" />
                                <Label TextColor="Black" Grid.Column="0" Grid.Row="0" Text="{Binding Amount}" />
                                <Label TextColor="Black" Grid.Column="0" Grid.Row="0" Text="{Binding Date}" />
                            </Grid>
                            </ViewCell>
                        </DataTemplate>
                     </ListView.ItemTemplate>
                </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>